<section class="section">
  <div class="columns is-vcentered">
    <div class="column is-narrow has-text-centered">
      <div class="prof-image">
        <figure class="image is-rounded is-inline-block">
          <%= render 'avatar', user: @user %>
        </figure>
      </div>
    </div>
    <div class="column">
      <%= turbo_frame_tag dom_id(@user, "edit-profile") do %>
        <div>
          <h1 class="title is-3"><%= @user.full_name %></h1>
          <strong><%= "Location: " %></strong><%= @user.location %><br>
          <strong><%= "Birthday: "%></strong><%= @user.birthday %><br>
          <strong><%= "Occupation: "%></strong><%= @user.occupation %><br>
          <strong><%= "College: "%></strong><%= @user.college %><br>
          <strong><%= "High School: "%></strong><%= @user.high_school %><br>
          <br>
        </div>
        <div class="is-flex is-align-items-center">
          <% if @user == current_user %>
            <%= link_to "Edit Profile", edit_user_path(current_user), data: { turbo_frame: dom_id(@user, "edit-profile") }, class: "button is-success is-small is-outlined" %>
          <% end %>
        </div>
      <% end %>
        <div class="is-flex is-align-items-center">
          <% if @user != current_user %>
            <%= render 'friendship_buttons', user: @user %>
          <% end %>
        </div>

    </div>
  </div>
</div>
</section>
