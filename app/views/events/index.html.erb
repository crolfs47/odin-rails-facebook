<div class="section">
  <h1 class="title is-5">Events</h1>
  <div class="section">
    <h2 class="title is-5 has-text-success">Events</h2>
    <div class="event-card-container">
      <% @events.each do |event| %>
        <%= render "card", event: event %>
      <% end %>
    </div>
  </div>
</div>
